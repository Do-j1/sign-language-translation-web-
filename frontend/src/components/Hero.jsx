function Hero() {
  return (
    <section className="hero">
      <h1>Translate Sign Language in Real Time</h1>
      <p>
        Choose the sign language first so the system can load the correct
        dataset or model, then continue to the live translation page.
      </p>
    </section>
  );
}

export default Hero;